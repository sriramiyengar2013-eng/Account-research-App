# Copy this to .env and fill in your Anthropic API key
# Netlify reads this automatically when running: netlify dev
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Only needed for standalone Express (npm run dev:server)
PORT=3001
